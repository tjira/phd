% arara: setup
% arara: lualatex: {options: ["--output-directory=build"]}
% arara: move: {files: ["build/main.pdf"], target: "main.pdf"}
% arara: biber: {options: ["--output-directory=build"]}
% arara: makeglossaries: {options: ["-d", "build"]}
% arara: lualatex: {options: ["--output-directory=build"]}
% arara: lualatex: {options: ["--output-directory=build"]}
% arara: move: {files: ["build/main.pdf"], target: "main.pdf"}
\directlua{if os.getenv("METADATA") == "1" then tex.print("\\input{frontmatter/metadata}") end}
\documentclass[headsepline=true,parskip=half,12pt]{scrbook}\raggedbottom
\title{Computational Strategies for Nonadiabatic Molecular Dynamics}\author{Tomáš \textsc{Jíra}}
\usepackage{style}
\input{frontmatter/functions}
\begin{document}
\directlua{if os.getenv("ENTRY") == "1" then tex.print("\\includepdf[pages=-]{frontmatter/pdf/entry}") end}
\directlua{if os.getenv("CHAPTER") == nil then tex.print("\\include{frontmatter/acknowledgements}") end}
\directlua{if os.getenv("CHAPTER") == nil then tex.print("\\include{frontmatter/abstract}") end}
\tableofcontents
\directlua{if os.getenv("CHAPTER") == nil then tex.print("\\include{frontmatter/introduction}") end}
\includechapters{chapters}
\directlua{if os.getenv("CHAPTER") == nil then tex.print("\\include{frontmatter/conclusion}") end}
\appendix
\includechapters{appendices}
\printglossary[type=\acronymtype]
\printbibliography[heading=bibintoc,title={References}]
\end{document}
